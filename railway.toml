[build]
builder = "nixpacks"

[deploy]
startCommand = "cd frontend && npx --yes serve -s dist -l tcp://0.0.0.0:$PORT"
restartPolicy = "on-failure"

[services.web]
# If you want to deploy the frontend as a specific web service
envVars = ["VITE_DIRECTUS_URL"]
